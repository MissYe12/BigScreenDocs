
缩进与换行
~~~~~~~~~~~~~~~


缩进
---------
使用 4个空格做为一个缩进层级

示例：

.. code-block:: sh
   :linenos:

   if (x < y) {
       x += 10;
   } else {
       x += 1;
   }


换行
----------
每行不得超过120个字符

解释：过长的代码不容易阅读与维护

